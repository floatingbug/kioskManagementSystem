const components = {
	toggleswitch: {
		colorScheme: {
			light: {
				background: "rgb(from var(--p-primary-color) r g b / 70%)",
				handle: {
					size: "1.2rem"
				}
			},
			dark: {
				background: "rgb(from var(--p-primary-color) r g b / 70%)",
				handle: {
					size: "1.2rem"
				}
			}
		}
	}
};


export default components;
